import 'package:flutter/material.dart';

class LoanEMICalculatorWidget extends StatelessWidget {
  const LoanEMICalculatorWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Loan/EMI Calculator - Coming Soon!'),
    );
  }
}
