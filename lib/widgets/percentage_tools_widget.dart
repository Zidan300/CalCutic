import 'package:flutter/material.dart';

class PercentageToolsWidget extends StatelessWidget {
  const PercentageToolsWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Percentage Tools - Coming Soon!'),
    );
  }
}
